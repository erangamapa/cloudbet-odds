import { MarketType } from './sports-core/sports-core-types';
export function throwIfInvalidMarketType(marketType) {
    if (!marketType || !Object.values(MarketType).includes(marketType)) {
        throw new Error("Got " + marketType + ", please pass in one of the supported marketType (" + Object.values(MarketType) + ")");
    }
}
export function throwIfUndefined(v, name) {
    if (typeof v === 'undefined') {
        throw new Error("Missing argument " + name);
    }
}
export function throwIfArrayEmpty(v, name) {
    if (!v || v.length < 1) {
        throw new Error("Missing argument " + name);
    }
}
//# sourceMappingURL=utility.js.map